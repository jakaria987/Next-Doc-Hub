/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.ibb.co', 'ibb.co', "images.unsplash.com"], // Add other domains if needed
    },
  };
  
  module.exports = nextConfig;
  